# Git Commands

## 1. git add .

This command adds all the changed files to the staging area.

## 2. git commit -m "message"

This command saves your changes with a message.

Example:
git commit -m "Added tasks file"

## 3. git push origin main

This command uploads your committed changes to GitHub.